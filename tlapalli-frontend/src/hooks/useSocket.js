// Hook de socket deshabilitado temporalmente
// Se implementará con un enfoque diferente más adelante
export default function useSocket(event, callback) {
  // No-op: la app funciona sin tiempo real
}
